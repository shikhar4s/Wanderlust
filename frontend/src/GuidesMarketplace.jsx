import React,{useEffect,useState} from 'react';
import {Link} from 'react-router-dom';
import {api} from './api';
import {MapPin,Star,Search,X} from 'lucide-react';

const list=data=>data?.results||data||[];
export default function GuidesMarketplace(){
 const [guides,setGuides]=useState([]),[trips,setTrips]=useState([]),[destination,setDestination]=useState(''),[city,setCity]=useState(''),[date,setDate]=useState(''),[from,setFrom]=useState(''),[until,setUntil]=useState(''),[selected,setSelected]=useState(null),[requestOpen,setRequestOpen]=useState(false),[slots,setSlots]=useState(null),[trip,setTrip]=useState(''),[service,setService]=useState(''),[people,setPeople]=useState(1),[note,setNote]=useState(''),[loading,setLoading]=useState(false),[message,setMessage]=useState('');
 useEffect(()=>{api('/trips/').then(d=>setTrips(list(d))).catch(e=>setMessage(e.message))},[]);
 useEffect(()=>{loadGuides()},[destination,date,from,until]);
 async function loadGuides(){setLoading(true);setMessage('');try{const params=new URLSearchParams();if(destination)params.set('destination',destination);if(date&&from&&until){params.set('date',date);params.set('start',new Date(`${date}T${from}`).toISOString());params.set('end',new Date(`${date}T${until}`).toISOString())}setGuides(list(await api(`/guides/?${params}`)))}catch(e){setMessage(e.message)}finally{setLoading(false)}}
 async function findCity(e){e.preventDefault();try{const result=await api(`/destinations/search/?q=${encodeURIComponent(city)}`);setDestination(String(result.id));setMessage(`Showing guides covering ${result.name}.`)}catch(e){setMessage(e.message)}}
 useEffect(()=>{if(selected&&date)api(`/guide-account/slots/?guide=${selected.id}&date=${date}`).then(setSlots).catch(()=>setSlots(null));else setSlots(null)},[selected,date]);
 const availableTrips=selected?trips.filter(t=>selected.coverage.some(c=>c.destination===t.destination)&&(!date||(t.start_date<=date&&date<=t.end_date))):trips;
 const chosenTrip=trips.find(t=>String(t.id)===String(trip));
 const requestServices=selected?.services.filter(s=>s.active&&(!chosenTrip||selected.coverage.some(c=>c.id===s.coverage&&c.destination===chosenTrip.destination)))||[];
 useEffect(()=>{if(requestServices.length&&!requestServices.some(s=>String(s.id)===String(service)))setService(requestServices[0].id)},[trip,selected]);
 function openRequest(guide){setSelected(guide);setRequestOpen(true);setService(guide.services.find(s=>s.active)?.id||'');setTrip('');setNote('')}
 async function sendRequest(e){e.preventDefault();setMessage('');try{if(!date||!from||!until)throw new Error('Choose a date and time first.');const result=await api('/requests/',{body:{guide:selected.id,service:Number(service),trip:Number(trip),start:new Date(`${date}T${from}`).toISOString(),end:new Date(`${date}T${until}`).toISOString(),people:Number(people),message:note}});setMessage(`Request #${result.id} sent. You can now message this guide.`);setRequestOpen(false)}catch(err){setMessage(err.message)}}
 return <div className="page"><div className="guideHero"><div><span className="kicker">LOCAL PERSPECTIVE</span><h1>Meet people who know the place</h1><p>Find a guide whose services cover your trip and who is available when you need them.</p></div></div><div className="guideFilters"><form onSubmit={findCity} className="inlineForm"><input aria-label="Search city" value={city} onChange={e=>setCity(e.target.value)} placeholder="Search a city…" required/><button><Search/> Find</button></form><label>Or choose a trip destination<select value={destination} onChange={e=>setDestination(e.target.value)}><option value="">All destinations</option>{trips.map(t=><option key={t.id} value={t.destination}>{t.destination_detail?.name}</option>)}</select></label><label>Date<input type="date" value={date} onChange={e=>setDate(e.target.value)}/></label><label>From<input type="time" value={from} onChange={e=>setFrom(e.target.value)}/></label><label>Until<input type="time" value={until} onChange={e=>setUntil(e.target.value)}/></label></div>{message&&<div className="statusLine">{message}</div>}{loading&&<p>Finding guides…</p>}{!loading&&!guides.length&&<div className="state">No guides match these filters yet. Try another date or destination.</div>}<div className="guideList">{guides.map(g=><article className="guide" key={g.id}>{g.photo_url?<img className="guidePhoto" src={g.photo_url} alt={g.name}/>:<div className="portrait">{g.name.split(' ').map(part=>part[0]).slice(0,2).join('')}</div>}<div className="guideInfo"><h3>{g.name}</h3><span className="reviewCount"><Star/> {g.rating?Number(g.rating).toFixed(1):'New'} · {g.review_count} reviews</span><b>{g.years_experience} years experience</b><p>{g.bio||'Local guide building their Wanderlust profile.'}</p><p>{(g.languages||[]).join(' · ')||'Languages not listed'}</p><div className="tags">{(g.specialties||[]).map(x=><span key={x}>{x}</span>)}</div><p><MapPin/> {g.coverage.map(c=>c.label).join(', ')||'Coverage not added'}</p></div><div className="guidePrice"><span className={g.accepting_bookings?'available':'unavailable'}>{g.accepting_bookings?'Accepting bookings':'Unavailable'}</span><b>{g.services.some(s=>s.active)?`From ₹${Math.min(...g.services.filter(s=>s.active).map(s=>Number(s.price)))}`:'No active service'}</b><button onClick={()=>{setSelected(g);setRequestOpen(false)}}>View profile</button><button className="primary" disabled={!g.accepting_bookings||!g.services.some(s=>s.active)} onClick={()=>openRequest(g)}>Request guide</button></div></article>)}</div>{selected&&<div className="modalBackdrop" onClick={()=>setSelected(null)}><div className="formPanel requestModal" onClick={e=>e.stopPropagation()}><button className="modalClose" onClick={()=>setSelected(null)} aria-label="Close"><X/></button><h2>{selected.name}</h2><p>{selected.bio}</p><p>{selected.languages.join(' · ')} · {selected.years_experience} years experience</p><h3>Services</h3>{selected.services.filter(s=>s.active).map(s=><div className="panelRow" key={s.id}><b>{s.title} · ₹{s.price}</b><span>{s.description} · {s.duration_minutes} min · {s.pricing_type}</span></div>)}{requestOpen&&<form onSubmit={sendRequest}><label>Service<select value={service} onChange={e=>setService(e.target.value)}>{requestServices.map(s=><option key={s.id} value={s.id}>{s.title} · ₹{s.price}</option>)}</select></label><label>Your trip<select required value={trip} onChange={e=>setTrip(e.target.value)}><option value="">Choose trip</option>{availableTrips.map(t=><option key={t.id} value={t.id}>{t.title}</option>)}</select></label>{!availableTrips.length&&<p><Link to="/discover">Create a trip</Link> in this guide’s coverage area first.</p>}<p>Requested time: {date||'Choose date above'} {from}–{until}</p>{slots&&<p>Available: {slots.slots.map(s=>`${new Date(s.start).toLocaleTimeString([], {hour:'2-digit',minute:'2-digit'})}–${new Date(s.end).toLocaleTimeString([], {hour:'2-digit',minute:'2-digit'})}`).join(', ')||'No available times'}</p>}<label>Group size<input type="number" min="1" value={people} onChange={e=>setPeople(e.target.value)}/></label><label>Message<textarea value={note} onChange={e=>setNote(e.target.value)}/></label><button className="primary" disabled={!availableTrips.length||!requestServices.length}>Send request</button></form>}{!requestOpen&&<button className="primary" disabled={!selected.accepting_bookings||!selected.services.some(s=>s.active)} onClick={()=>setRequestOpen(true)}>Request this guide</button>}</div></div>}</div>
}
